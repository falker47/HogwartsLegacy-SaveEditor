# Hogwarts Legacy Save Editor - Dependencies

# GUI framework
customtkinter>=5.0.0,<6.0.0

# WebView for the save editor interface
pywebview>=4.0.0,<6.0.0

# Optional: Drag-and-drop support (gracefully handled if missing)
tkinterdnd2>=0.3.0,<1.0.0
